package in.supplybase.backend.booking;

public enum BookingType {
    /** A smaller, well-understood job. The customer expects someone to turn up. */
    SERVICE,
    /** Interiors, construction, renovation. The visit exists to produce a quotation. */
    PROJECT
}
